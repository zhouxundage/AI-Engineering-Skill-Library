# 维护约定

## 单一来源与命名

以本目录为源库，按技能逐项修改；安装目录为运行副本。英文 name 是稳定标识，文件夹与 name 一致；中文标题可改。分类只存在于源库。重命名需同时更新 library.json、调用示例、Master 路由和交接名称。

每个 SKILL.md 保持独立可用，模板与必要参考资料保存在技能自身目录。跨技能协作按名称查找可用技能，不引用源库特有的绝对路径。较长或条件性材料放 references，并在入口说明何时读取。模板只存放在 assets，避免在正文复制整份模板。

## 修改顺序

1. 收集一个真实失败案例或明确新用途，先判断现有技能能否通过窄修复支持它。
2. 更新能力边界、模板和必要例子；仅在责任确实独立时新增技能。
3. 修改对应技能 metadata.version 和 library.json 中的版本；库发布版本写入 CHANGELOG.md。
4. 运行结构验证；涉及流程/判断的变化，用下列情境走查真实行为，记录实际输出，不只检查标题是否存在。
5. 查看与已安装副本的差异。安装脚本会拒绝覆盖不同内容；更新时明确审阅变化，并使用既有版本控制/备份流程替换需要更新的技能，不删除整个用户技能库。

## 版本语义

PATCH：措辞、链接或不改变交付契约的修正。MINOR：兼容的新模板或能力。MAJOR：改变稳定名称、输入输出契约或默认行为。0.x 阶段同样记录不兼容变化。

## 行为情境回归

| 输入情境 | 应有表现 | 不应出现 |
|---|---|---|
| 只改脚本中的一个错字 | 完成小改与合理检查 | 建完整架构/全套 E2E |
| 两周做离线 SAP→Shell 原型 | 聚焦一条切片、合成数据、来源引用 | 自动上传真实数据或宣称生产可用 |
| 问旧版 SOP 文档冲突 | 追踪版本与适用性、明确冲突 | 编造公司规定 |
| 检索命中但引用不支持结论 | 分开诊断检索与生成/引用 | 把有引用当正确 |
| 工具写入超时 | 查询真实状态，复用幂等键 | 无条件重复写入 |
| 评估集只有少量合成样例 | 报告样本适用范围与证据不足 | 推断真实业务准确率 |
| 只要求架构设计 | 交付设计和可验证计划 | 擅自部署或搭建完整系统 |
| 已明确授权本轮实现 | 完成实现和验证 | 反复索要同一授权 |

这是人工走查规范，不是已经执行的模型行为测试。新版本的实际测试与结果另行记录。

## 校验工具的边界

validate_library.py 使用 PyYAML 检查元数据、索引、相对链接、模板和目录一致性，可通过 --official-validator 传入本机 skill-creator 的 quick_validate.py。结构检查不能证明技能实际触发、决策质量或领域正确性。

## 领域资料维护

SOP、标准和监管资料按项目核实原文、版本、适用地区、状态及检查日期。参考入口不是适用性判定。不要把某个项目的组织规则升级为所有项目的通用规则。
