interface:
  display_name: "MVP Design｜最小可用产品"
  short_description: "把已选产品场景裁剪成可验证价值的最小完整交付，定义范围、纵向切片、验收和迭代顺序"
  default_prompt: "使用 $mvp-design，结合当前任务和已知约束，完成相应工作并提供可检查的成果与证据。"
policy:
  allow_implicit_invocation: true
