# 模块实现卡

功能/需求 ID：{{feature_id}}  契约版本：{{contract_version}}
运行时/依赖锁：{{runtime_lock}}  配置入口：{{config}}
主要文件及责任：{{files}}
输入/输出/错误：{{contract}}
调用入口与实际使用示例：{{entry_example}}
注册：不适用 / {{registry_entry_and_collision_policy}}
日志字段与敏感信息处理：{{logging}}

## 可复现操作

安装：{{install_command}}
运行：{{run_command}}
示例输入：{{sample_input}}
期望可观察输出：{{expected_output}}

| 验证 | 环境/命令 | 实际结果 | 证据 | 状态 |
|---|---|---|---|---|
| {{check}} | {{command}} | {{result}} | {{artifact}} | 未运行 |

接口变更与兼容性：{{compatibility}}
未完成项及影响：{{remaining}}
